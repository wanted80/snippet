name: Pages

on:
  release:
    types:
      - published

permissions:
  contents: read

jobs:
  pages:
    name: Build GitHub Pages
    if: github.repository == 'wanted80/snippet' && github.event.release.prerelease == false
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read

    steps:
      - name: Check out tested revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.release.tag_name }}

      - name: Validate and build the composed demo site
        run: |
          make builder-image
          sh docker/demo/check.sh snippet-builder:smoke "$RUNNER_TEMP/snippet-public"

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: ${{ runner.temp }}/snippet-public/

  deploy:
    name: Deploy GitHub Pages
    if: github.repository == 'wanted80/snippet' && github.event.release.prerelease == false
    needs: pages
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    concurrency:
      group: github-pages
      cancel-in-progress: false
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
